# =============================================================================
# DSSM Docker Configuration
# =============================================================================

# Security (REQUIRED - change in production!)
# Generate with: openssl rand -base64 32
JWT_SECRET=change-this-to-a-secure-random-secret

# WebAuthn Configuration
# For localhost development
RP_ID=localhost
RP_NAME=Dead Simple Secret Manager

# For production with custom domain
# RP_ID=vault.yourdomain.com
# RP_NAME=My Secret Vault

# Server Configuration
PORT=3000
NODE_ENV=production

# Database (default paths work out of the box)
DB_PATH=/app/data/dssm.db

# =============================================================================
# Development Mode (Optional)
# =============================================================================
# Uncomment for local development
# NODE_ENV=development
# PORT=8787
# DB_PATH=./data/dssm.db
